// Runtime configuration — edit this file on the deployed server WITHOUT rebuilding.
// Set apiUrl to the address where the FastAPI backend is reachable from the browser.
// Leave it empty ("") to fall back to the build-time VITE_API_URL, then to localhost.
// Examples:
//   window.__APP_CONFIG__ = { apiUrl: "https://api.your-domain.com" };
//   window.__APP_CONFIG__ = { apiUrl: "http://203.0.113.10:8020" };
window.__APP_CONFIG__ = { apiUrl: "" };
